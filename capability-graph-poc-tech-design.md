# Capability Graph PoC — Technical Design

The maintained technical design is [`docs/tech-design.md`](docs/tech-design.md).
This compatibility pointer replaces an earlier byte-for-byte duplicate so the
dataset facts, benchmark invariants, and implementation status cannot drift.
